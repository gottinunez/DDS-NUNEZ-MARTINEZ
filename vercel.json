{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "build"
        }
      }
    ],
    "env": {
      "BUN_INSTALL": "true"
    },
    "buildCommand": "npm install --legacy-peer-deps && bun build"
  }
  